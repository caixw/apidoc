# Copyright 2018 by caixw, All rights reserved.
# Use of this source code is governed by a MIT
# license that can be found in the LICENSE file.


# 以下定义了每个接口定义需要的内容

@api GET /users/{id}/logs 获取用户的日志
group: g1 # 所在的分组
tags: [t1,t2]
deprecated: false
description: |markdown
  == markdown
  == markdown
operationID: getuserid
queries:
  page: int default desc
  size: int default desc markdown
  state: array [normal,lock] [normal,lock,delete] 状态码
params:
  id: int desc markdown
headers:
  content-type: desc
  authorization: auth
request:
  description: request body
  content:
    application/json:
      schema:
        type: object
        properties:
          username:
            type: string
            description: 登录账号
          password:
            type: string
            description: 密码
responses:
  default:
    description:
    headers:
      header1: header1
      header2: header2
    content:
      application/json:
        schema:
          type: object
          properties:
            token:
              type: string
              description: 登录账号
            password:
              type: string
              description: 密码
